'use client';

export default function NewCampaignPage() {
  return (
    <div className="mx-auto max-w-3xl py-10 text-center">
      <h1 className="text-[24px] font-extrabold text-ink">Create a campaign</h1>
      <p className="mt-2 text-muted">The 5-step campaign wizard (Brief → Assets → Targeting → Quote → Fund) is being built next.</p>
    </div>
  );
}
